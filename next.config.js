/** @type {import('next').NextConfig} */
const nextConfig = {
  // For Next.js 13+ with App Router, server actions and API routes
  // handle their own request size limits
  // Key optimization: removed raw_data from deal creation to minimize payload
}
module.exports = nextConfig
